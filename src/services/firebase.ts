// This file is no longer needed for direct Firebase configuration
// We'll keep it for type definitions if needed in the future

import { FirebaseApp } from 'firebase/app';
import { Auth } from 'firebase/auth';
import { Firestore } from 'firebase/firestore';

// These are just type definitions, not actual instances
export type { FirebaseApp, Auth, Firestore };

// Remove all Firebase initialization code

// export {}